class MyBenchmark
  def bm(left, right)
  end

  def bm_instances(left, right, method:)
  end

  private

  # You could use `Time.now`
  # You don't have to use this method though, it's just a hint!
  def time(callable)
  end
end
